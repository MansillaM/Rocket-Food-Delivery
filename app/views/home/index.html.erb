<%= render 'layouts/navigationbar' %>

<div class="container mt-5">
    <div class="row">
      <div class="col-md-6">
        <h1>Welcome to Rocket Food Delivery!</h1>
        <p>Order food from your favorite local restaurants with Rocket Food Delivery. Browse menus, choose your items, and track your order in real time.</p>
        <% if user_signed_in? %>
          <%= link_to "Order Food Now", restaurants_path, class: "btn btn-primary btn-lg" %>
        <% else %>
          <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary btn-lg" %>
        <% end %>
      </div>
      <div class="col-md-6">
        <%# <%= image_tag "delivery.jpg", class: "img-fluid rounded" %> 
      </div>
    </div>
  </div>